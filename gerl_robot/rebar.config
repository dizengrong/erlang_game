{erl_opts, [
	%% 设置编译选项
	% {src_dirs, ["src", "tool", "config"]},
	%% 使用lager日志库需要加的编译选项，这样才可以使用lager:info等方法的
	{parse_transform, lager_transform}, 
	{i, "hrl"},
	warnings_as_errors, 
	debug_info
]}.

%% 依赖
{deps, [lager]}.